spring:
  config:
    import: optional:file:.env.local[.properties]
security:
  allowed-origins:
    - http://localhost:3000
  jwt:
    refresh-token:
      expire-time: 60
      secure: false

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE