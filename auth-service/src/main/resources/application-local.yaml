spring:
  config:
    import: optional:file:.env.local[.properties]
  datasource:
    url: jdbc:mysql://localhost:13306/hcp_user?serverTimezone=UTC&characterEncoding=UTF-8
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE